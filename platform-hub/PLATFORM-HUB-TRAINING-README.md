# Nexus Health Insurance AI Platform — Independent Training Pages

Contains 12 independent Flask/Jinja training pages, a replacement training index, and an updated `routes.py`.

Copy `routes.py` to your hub package, `templates/training_index.html` to templates, and `templates/training/` to your templates directory.

The pages assume Bootstrap 5 and Bootstrap Icons are already loaded by `base.html`.
